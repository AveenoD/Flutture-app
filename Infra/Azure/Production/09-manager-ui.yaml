apiVersion: v1
kind: Service
metadata:
  name: crownco-manager-ui
  namespace: crownco-production
spec:
  type: ClusterIP
  selector:
    app: crownco-manager-ui
  ports:
    - name: http
      port: 80
      targetPort: 3000
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: crownco-manager-ui
  namespace: crownco-production
spec:
  replicas: 2
  selector:
    matchLabels:
      app: crownco-manager-ui
  template:
    metadata:
      labels:
        app: crownco-manager-ui
    spec:
      imagePullSecrets:
        - name: acr-pull-secret
      containers:
        - name: manager-ui
          image: crownco/manager-ui:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 3000
          envFrom:
            - configMapRef:
                name: crownco-ui-config
          readinessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 30
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            requests:
              cpu: 50m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 1Gi

apiVersion: v1
kind: Service
metadata:
  name: crownco-presales-ui
  namespace: crownco-production
spec:
  type: ClusterIP
  selector:
    app: crownco-presales-ui
  ports:
    - name: http
      port: 80
      targetPort: 3000
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: crownco-presales-ui
  namespace: crownco-production
spec:
  replicas: 2
  selector:
    matchLabels:
      app: crownco-presales-ui
  template:
    metadata:
      labels:
        app: crownco-presales-ui
    spec:
      imagePullSecrets:
        - name: acr-pull-secret
      containers:
        - name: presales-ui
          image: crownco/presales-ui:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 3000
          envFrom:
            - configMapRef:
                name: crownco-ui-config
          readinessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 30
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            requests:
              cpu: 50m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 1Gi

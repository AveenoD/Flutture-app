# Docker Postgres is exposed on port 5433 (see Infra/Compose/docker-compose.yml)
# From host, use POSTGRES_PORT=5433 when running the backend.
POSTGRES_HOST=localhost
POSTGRES_PORT=5433
POSTGRES_USER=crownco-db-user
POSTGRES_PASSWORD=crownco-db-password
POSTGRES_DB=crownco-db

PORT=3000
